TODOS BACKEND: 
1. Añadir un "express-rate-limit" así evitamos spam, por ejemplo “máximo 3 listas por minuto por IP”. Lo mismo con los todos



-------------------------------------------------------------------------
TODOS LLUVIA DE IDEAS PRE PROGRAMACIÓN:

1. Función que tras usarse cree una nueva lista con una ID única.

Las funciones que describo a continuación serán para una lista en particular (seguro uno de los parámetros termina siendo el link).
2. Función que permite crear nuevas tareas "created".
2a. función que permita consultar las tareas en estado created
3. Función que permite asignar tareas como "completed".
3a. función que permita consultar las tareas en estado completed
4. Función que permita eliminar una tarea "created" o "completed". "eliminated".
5. Función que permita consultar las tareas en estado "eliminated".
6. Función que permita restaurar las tareas en estado "eliminated", pasando estas a "created". 
7. Función que permita marcar todas las tareas "created" como "completed".
8. Función que permita eliminar todas las tareas, ya sean "created" o "completed".

Si surgen otras funciones las veremos a futuro, pero de momento son esas. Si pensás que es mejor hacer una función que cubra varias de las funcionalidades descritas, hacelo; siempre y cuando dse respete la mantenibilidad del código. 